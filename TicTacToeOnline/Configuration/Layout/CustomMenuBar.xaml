<UserControl
    x:Class="TicTacToeOnline.Configuration.Layout.CustomMenuBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TicTacToeOnline.Configuration.Layout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Background="Transparent">
                <MenuBar>
                    <MenuBarItem Title="Game">
                        <MenuFlyoutItem Text="Request New" IsEnabled="False"/>
                        <MenuFlyoutItem x:Name="LeaveGameButton" Text="Leave" IsEnabled="False"/>
                        <MenuFlyoutItem Text="Choose Name" IsEnabled="False"/>
                        <MenuFlyoutItem x:Name="HostGameButton" Text="Host Game" IsEnabled="False"/>
                        <MenuFlyoutItem x:Name="JoinGameButton" Text="Join Game" IsEnabled="False"/>
                    </MenuBarItem>
                </MenuBar>
            </Grid>
        </Grid>
    </UserControl>
</UserControl>
